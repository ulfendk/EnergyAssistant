@page "/integrations"
@using EnergyAssistant.Components
@using UlfenDk.EnergyAssistant.Config

<PageTitle>Integrations</PageTitle>

<MudText Typo="Typo.h3" GutterBottom="true">Integrations</MudText>
<MudText Typo="Typo.subtitle1">Configure integrations</MudText>

<HomeAssistantIntegrationConfiguration Class="ma-6" />

<CarnotIntegrationConfiguration Class="ma-6" />

<EnergiDataServiceIntegrationConfiguration Class="ma-6" />

<NordpoolIntegrationConfiguration  Class="ma-6"/>

<ElOverblikIntegrationConfiguration  Class="ma-6" />

@code {
    bool success;
    string[] errors = { };
    MudTextField<string> pwField1;
    MudForm form;

    private IEnumerable<string> PasswordStrength(string pw)
    {
        if (string.IsNullOrWhiteSpace(pw))
        {
            yield return "API key is required!";
            yield break;
        }
    }
}
